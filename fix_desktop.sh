#!/usr/bin/env bash

TGT="$HOME/.config/plasma-org.kde.plasma.desktop-appletsrc"
# ARG1="$1"
# SRCH1="\["
# declare -A HEADS=$(grep --extended-regex "$SRCH1" "$TGT" --color=never | sort) #--only-matching --line-number 
#echo $HEADS|cut -d[ -f1
# for ENTRY in ${HEADS[@]}; do
# 	echo $ENTRY
# done
echo "
You gotta faff around in here to fix it i think:
    $TGT
"

exit 0